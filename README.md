Overview

- Download the code files onto your machine
- Create a `data/` directory locally and edit the file paths at the end of `params.py` so that they point to this directory
- Run the diffusion code from `main.py`

